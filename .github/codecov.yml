# code coverage configurations
coverage:
  status:
    # measures overall project coverage
    project:
      default:
        # how much decrease in coverage is allowed
        threshold: 50%

    # measures PR or single commit coverage
    patch:
      default:
        # how much decrease in coverage is allowed
        threshold: 50%
